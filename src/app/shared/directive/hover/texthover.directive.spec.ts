import { TexthoverDirective } from './texthover.directive';

describe('TexthoverDirective', () => {
  it('should create an instance', () => {
    const directive = new TexthoverDirective();
    expect(directive).toBeTruthy();
  });
});
