import { BghoverDirective } from './bghover.directive';

describe('BghoverDirective', () => {
  it('should create an instance', () => {
    const directive = new BghoverDirective();
    expect(directive).toBeTruthy();
  });
});
